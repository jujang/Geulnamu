package com.geulnamu.domain.actionHistory;

public enum ApiMethod {
    POST,
    GET,
    PATCH,
    DELETE,

    // 안 쓰겠지만 일단은 표준에 정의되어 있기에 넣어둠
    PUT,
    HEAD,
    OPTIONS,
    TRACE,
    CONNECT
}
